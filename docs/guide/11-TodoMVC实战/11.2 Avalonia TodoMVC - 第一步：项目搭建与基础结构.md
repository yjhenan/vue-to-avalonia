## 11.2 Avalonia TodoMVC - 第一步：项目搭建与基础结构

### 11.2.1 创建项目

```bash
# 创建Avalonia项目
dotnet new avalonia.app -n TodoMVC

# 进入项目目录
cd TodoMVC

# 安装依赖
dotnet add package CommunityToolkit.Mvvm
dotnet add package Newtonsoft.Json
```

### 11.2.2 项目结构

```
TodoMVC/
├── Models/
│   └── TodoItem.cs
├── ViewModels/
│   ├── MainViewModel.cs
│   └── ViewModelBase.cs
├── Views/
│   └── MainWindow.axaml
├── Services/
│   ├── TodoService.cs
│   └── ITodoService.cs
├── App.axaml
└── Program.cs
```

### 11.2.3 数据模型

```csharp
// Models/TodoItem.cs
using CommunityToolkit.Mvvm.ComponentModel;

namespace TodoMVC.Models
{
    public partial class TodoItem : ObservableObject
    {
        public long Id { get; set; }

        [ObservableProperty]
        private string title = string.Empty;

        [ObservableProperty]
        private bool completed = false;

        public TodoItem Clone()
        {
            return new TodoItem
            {
                Id = this.Id,
                Title = this.Title,
                Completed = this.Completed
            };
        }
    }
}
```

### 11.2.4 服务接口

```csharp
// Services/ITodoService.cs
using System.Collections.ObjectModel;
using System.Threading.Tasks;

namespace TodoMVC.Services
{
    public interface ITodoService
    {
        ObservableCollection<TodoItem> Todos { get; }
        Task LoadAsync();
        Task SaveAsync();
        void Add(string title);
        void Remove(long id);
        void Update(TodoItem item);
        void ClearCompleted();
    }
}
```

**下一步将实现：TodoService和主界面**

---

**本节完成内容：**
✅ 项目结构设计
✅ TodoItem模型定义
✅ ITodoService接口定义

**下一节将实现：**
- TodoService数据持久化
- MainWindow基础布局


# 第十一章：TodoMVC完整实现（完整版）
